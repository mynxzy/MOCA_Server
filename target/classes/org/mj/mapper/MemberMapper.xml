<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.mj.mapper.MemberMapper">

  <insert id="MemberInsert">
  	insert into member(u_name, u_id, u_pw, u_nick, u_tel, u_email, u_address)
  	values(#{u_name}, #{u_id}, #{u_pw}, #{u_nick}, #{u_tel}, #{u_email}, #{u_address})
  </insert>
  
  <select id="Login" resultType="org.mj.model.MemberDTO">
 	select * from member where u_id=#{u_id} and u_pw=#{u_pw}
  </select>
</mapper>